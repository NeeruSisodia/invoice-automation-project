# invoice-automation-project
This project automates the processing of paper-based invoices. PDF invoices are read, data is extracted using UiPath, validated using Robot Framework, and stored in a MySQL database. The system verifies IBAN, reference numbers, and checks that line item totals match the invoice total.
